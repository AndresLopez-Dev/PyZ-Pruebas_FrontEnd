kind: pipeline
type: docker
name: default

steps:
- name: test
  image: node
  commands:
  - npm ci
  - npm run lint

- name: build
  image: node
  commands:
  - npm run build
  - echo build pyz app

- name: greeting
  image: alpine
  environment:
    USERNAME: 
      from_secret: user_name
  commands:
  - echo $USERNAME
  - echo world
  - pwd
  - ls -la

# - name: start
#   image: node
#   commands:
#   - npm run start