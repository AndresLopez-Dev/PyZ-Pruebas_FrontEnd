kind: pipeline
type: docker
name: default

steps:
- name: test
  image: node
  commands:
  - npm ci
  - npm run lint

- name: build
  image: node
  commands:
  - npm run build
  - echo build pyz app
  - npm run start

- name: greeting
  image: alpine
  commands:
  - echo hello
  - echo world
  - pwd
  - ls -la

  # - name: start
  # image: node
  # commands:
  # - npm run start