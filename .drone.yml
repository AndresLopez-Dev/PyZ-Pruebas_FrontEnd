kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: greeting
  commands:
  - echo hello world

# kind: pipeline
# name: default

# steps:
# - name: test
#   image: node
#   commands:
#   - npm install
#   - npm run lint

